# Magma

Docker orb board. OpenCode: load the `magma` skill for HTTP/`magma.sh`.

API: `http://127.0.0.1:3100/api`. The `magma` container is protected. Prefer `bun test test` in `scripts/` for unit tests.
